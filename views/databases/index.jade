extends ../layout
block content
  #databases
    h1 Databases
    - each db in databases
      .database
        p.name
          a(href="/databases/#{db._id}")= db.name
    hr
    .actions
      p
        a(href="#") New Database

